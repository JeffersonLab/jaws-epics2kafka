plugins {
    id 'java-library'
}

repositories {
    jcenter()
}

dependencies {
    api group: 'org.apache.kafka', name: 'connect-transforms', version: '2.4.0'

    testImplementation 'junit:junit:4.12'
}
