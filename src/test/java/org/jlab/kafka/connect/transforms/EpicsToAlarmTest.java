package org.jlab.kafka.connect.transforms;

import org.apache.kafka.connect.connector.ConnectRecord;
import org.junit.Test;
import static org.junit.Assert.*;

public class EpicsToAlarmTest {
    @Test public void testSomeLibraryMethod() {
        EpicsToAlarm.Value classUnderTest = new EpicsToAlarm.Value();

        //ConnectRecord record = new ConnectRecord();

        //classUnderTest.operatingValue(record);
    }
}
